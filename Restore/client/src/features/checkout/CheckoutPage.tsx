import { Typography } from "@mui/material";
import React from "react";

const CheckoutPage = () => {
  return (
    <Typography variant="h3">
      Only logged in users should be able to ses this1
    </Typography>
  );
};

export default CheckoutPage;
